overwrite: true
schema: ./src/schema.graphql
hooks:
  afterOneFileWrite:
    - prettier --write
generates:
  src/_generated/documents.graphql:
    plugins:
      - "@linear/doc-plugin"
  src/_generated/sdk-jsdoc.ts:
    plugins:
      - jsdoc
