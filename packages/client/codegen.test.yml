overwrite: true
schema: "../sdk/src/schema.graphql"
documents: "../sdk/src/**"
hooks:
  afterOneFileWrite:
    - prettier --write
generates:
  src/_tests/_generated.test.ts:
    plugins:
      - "@linear/codegen-test"
