overwrite: true
schema: ./src/schema.graphql
hooks:
  afterOneFileWrite:
    - prettier --write
generates:
  src/_generated/doc.graphql:
    plugins:
      - "@linear/doc-plugin"
  src/_generated/sdk-jsdoc.ts:
    plugins:
      - jsdoc
