{
  "name": "@linear/common",
  "version": "1.0.0",
  "author": "Linear Orbit, Inc",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=12.x",
    "yarn": "1.x"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev:common": "NODE_ENV=development run-s build:common dev:common:push",
    "dev:common:push": "yalc push",
    "build:common": "yarn build:common:clean && npx tsc",
    "build:common:watch": "yarn build:common --watch",
    "build:common:clean": "npx rimraf dist"
  },
  "dependencies": {
    "tracer": "1.1.4",
    "typescript": "4.0.5"
  }
}
