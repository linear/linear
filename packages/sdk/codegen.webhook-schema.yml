overwrite: true
schema: ./Linear-Webhooks@current.graphql
hooks:
  afterOneFileWrite:
    - prettier --write
generates:
  src/schema-webhook.json:
    plugins:
      - introspection
  src/schema-webhook.graphql:
    plugins:
      - schema-ast
    config:
      includeDirectives: true
      sort: true
