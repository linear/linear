overwrite: false
schema: ./Linear-Webhooks@current.graphql
hooks:
  afterOneFileWrite:
    - prettier --write
generates:
  src/schema.json:
    plugins:
      - introspection
  src/schema.graphql:
    plugins:
      - schema-ast
    config:
      includeDirectives: true
      sort: true
