# Get all connected teams
query teams {
  teams {
    nodes {
      id
      name
    }
  }
}

# Get one team by id
query team($teamId: String!) {
  team(id: $teamId) {
    id
    name

    issues {
      nodes {
        id
        title
        description
        assignee {
          id
          name
        }
        createdAt
        archivedAt
      }
    }
  }
}
