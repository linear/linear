---
"@linear/codegen-test": minor
---

Added support for generating mock values for GraphQL input object types (with proper handling of required fields, nested objects, enums, and list types) in test mock generation.
