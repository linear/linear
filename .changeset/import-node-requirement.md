---
"@linear/import": major
---

**`linear-import` now requires Node.js 20.19+ or 22.12+**, because `@linear/sdk` is now ESM-only. On older Node.js versions, upgrade Node.js or keep using `@linear/import@3`.
